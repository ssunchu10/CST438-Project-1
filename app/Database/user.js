export default function User(email, familyName, givenName, name, id) {
    this.email = email;
    this.familyName = familyName;
    this.givenName = givenName;
    this.name = name;
    this.id = id;
  }